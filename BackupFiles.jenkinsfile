
@Library('jenkins-sharedlibs')
import org.ballab1.pipeline.AllNodes


def tarFiles = { nodeName ->
  def tarfile = nodeName + '.cfg.tar.gz'
  sh "tar -cvzf ${tarfile} ./tarfiles.lst || true"
  archiveArtifacts "${tarfile}"
}

def helper = new AllNodes()
helper.process(this, tarFiles)

